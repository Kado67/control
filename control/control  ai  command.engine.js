// ui/control_ai_command.engine.js

// InflowAI Control AI Command Engine
// Şimdilik sadece iskelet. İleride Ortak'a verilecek komutları
// yorumlayıp kontrol merkezine yeni kartlar / kurallar eklemek için
// kullanılabilir.

export function handleOrtakCommand(commandText) {
  // Örn: "Bugünkü trafiği detaylı göster"
  console.log("[ORTAK KOMUT]", commandText);
  // Burada gelecekte:
  // - Komutu NLP ile analiz et
  // - Uygun UI bileşenini seç
  // - Dashboard'a yeni kart ekle / veri kaynağı bağla
}
